# Copy to .env and fill in. Never commit .env.

# Fallback credentials used when users.csv is absent and run.users == 1.
ROOT_EMAIL=
ROOT_PASSWORD=

# Aurora MySQL password for db_check.py (Phase 8). Leave blank while db.enabled is false.
SECURITY_BOT_DB_PASSWORD=
