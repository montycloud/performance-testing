Extract findings per selected tenant (no aggregation). Output starts with:Title - #Tenant Resource Inventory Snapshot – AWS Accounts, Regions, and Top Resource Types across Compute, Storage, Networking, and Database.For each tenant:2–4 line summary (total resources + highlights) for each tenant.Table having –By Account→ # | Account Number| Total ResourcesBy Region→ # | Region Code | Region Name | Count A Markdown table for:Top Resource Types for Compute (e.g., EC2, ECS, Lambda, Fargate, EKS)→ ## | Resource Type | Count | DescriptionTop Resource Types for Storage (e.g., S3, EBS, EFS, Storage Gateway, Snow Family)→ ## | Resource Type | Count | DescriptionTop Resource Types for Networking (e.g., VPC, Transit Gateway, ELB, Direct Connect, CloudFront)→ ## | Resource Type | Count | DescriptionTop Resource Types for Database (e.g., RDS, Aurora, DynamoDB, Redshift, Neptune, DocumentDB, ElastiCache, Keyspaces)→ ## | Resource Type | Count | DescriptionAfter each table: add two short "This table demonstrates…" lines. Continue past errors with a note, avoid duplicates. Use tool calls to fetch account and tenant names.Note - 1. Each section should only include resource types that belong to that category. Do not mix (e.g., SQS should not appear under Database and IAM should not be part of Networking).2. Should not print the Tenant ID, it should only show the Tenant Name.Total Resources must mean the actual total number of unique resources for that tenant only. Do not calculate it by summing counts from the By Account table, By Region table, or Top Resource Types table, because those are breakdown views of the same inventory and can cause double counting.MANDATORY: Do not ask for any user confirmations after each domain. Proceed with fetching everything asked and present it as requested.