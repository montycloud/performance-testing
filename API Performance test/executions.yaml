# Chat query sweep manifest — consumed by run_chat_executions.py
#
# Runs the same Locust chat test multiple times back-to-back with a different
# chat_queries file (and optionally a different description) each time, while
# keeping users/concurrency identical across every run. config.yaml itself is
# never permanently changed — run_chat_executions.py overwrites it per-run and
# restores the original content once the whole sweep finishes.
#
# Paths under `queries_file` are resolved the same way config.yaml's
# `chat.queries_file` normally is: relative to this "API Performance test/"
# directory (or absolute).

# Shared across every execution in this sweep ("users remain the same").
run:
  users: 1
  spawn_rate: 1
  run_time: ""   # blank = rely on single_journey mode's auto-stop (see config.yaml)

executions:
  # Runnable out of the box — reuses the existing sample queries file twice
  # under different names so you can dry-run/verify the sweep mechanics.
  - name: report_AI_Chat_User_1_Tenants_3_High_Query_wafr_1
    queries_file: "./chat_queries/chat_queries_wafr_1.txt"
    description: |
      AI Chat, 
      Parallel User: 1
      Tenants 3  ( MSP ,Tenant 1 and Tenant 2 )
      Extract findings data for Selected Tenant(s) and assessment "Default WAFR Scan
      

  - name: report_AI_Chat_User_1_Tenants_3_Medium_Query_wafr_2
    queries_file: "./chat_queries/chat_queries_wafr_2.txt"
    description: |
      AI Chat, 
      Parallel User: 1
      Tenants 3  ( MSP ,Tenant 1 and Tenant 2 )
      Get the Critical Severity finding counts for the Security pillar across all assessments in selected Tenant's

  - name: report_AI_Chat_User_1_Tenants_3_Medium_Query_wafr_3
    queries_file: "./chat_queries/chat_queries_wafr_3.txt"
    description: |
      AI Chat, 
      Parallel User: 1
      Tenants 3  ( MSP ,Tenant 1 and Tenant 2 )
      Get the Critical Severity finding counts for the Cost Optimization pillar across all assessments in selected Tenant's

  - name: report_AI_Chat_User_1_Tenants_3_Medium_Query_wafr_4
    queries_file: "./chat_queries/chat_queries_wafr_4.txt"
    description: |
      AI Chat, 
      Parallel User: 1
      Tenants 3  ( MSP ,Tenant 1 and Tenant 2 )
      Show me the Top Violating Checks from "Default WAFR" assessment across the Pillars for selected Tenant


  - name: report_AI_Chat_User_1_Tenants_3_Medium_Query_wafr_5
    queries_file: "./chat_queries/chat_queries_wafr_5.txt"
    description: |
      AI Chat, 
      Parallel User: 1
      Tenants 3  ( MSP ,Tenant 1 and Tenant 2 )
      Get the Critical and High Severity finding counts for the Operational Excellence pillar across all assessments in selected Tenant's

  - name: report_AI_Chat_User_1_Tenants_3_Medium_Query_wafr_6
    queries_file: "./chat_queries/chat_queries_wafr_6.txt"
    description: |
      AI Chat, 
      Parallel User: 1
      Tenants 3  ( MSP ,Tenant 1 and Tenant 2 )
      Get the Critical and High Severity finding counts for the Reliability pillar across all assessments in selected Tenant's,

  - name: report_AI_Chat_User_1_Tenants_3_low_Query_wafr_7
    queries_file: "./chat_queries/chat_queries_wafr_7.txt"
    description: |
      AI Chat, 
      Parallel User: 1
      Tenants 3  ( MSP ,Tenant 1 and Tenant 2 )
      Get the Critical and High Severity finding counts for the Performance Efficiency pillar across all assessments in selected Tenant's


  - name: report_AI_Chat_User_1_Tenants_3_Medium_Query_wafr_8
    queries_file: "./chat_queries/chat_queries_wafr_8.txt"
    description: |
      AI Chat, 
      Parallel User: 1
      Tenants 3  ( MSP ,Tenant 1 and Tenant 2 )
      Get the Critical and High Severity finding counts for the Sustainability pillar across all assessments in selected Tenant's

  # Template for a real multi-type sweep — copy one file per query "type",
  # point queries_file at it, give each entry a distinct name, then uncomment.
  # - name: net_cost_2_months
  #   queries_file: "./chat_queries_net_cost.txt"
  #   description: |
  #     AI Conversation Chat — Tenant Net Cost Report query set
  #
  # - name: security_pillar
  #   queries_file: "./chat_queries_security_pillar.txt"
  #   description: |
  #     AI Conversation Chat — Security Pillar WAFR query set
